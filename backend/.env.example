# Required for embeddings and LLM
OPENAI_API_KEY=sk-...

# Optional: for live web search (Phase 4+)
SERP_API_KEY=...

# Optional overrides
# CLAIM_MAX_LENGTH=2000
# DECOMPOSE_ENABLED=true
# DECOMPOSE_MIN_CLAIM_LENGTH=20  # min length to call decomposition LLM; below this, treat as single claim
# DECOMPOSE_MAX_SUBCLAIMS=5
# DECOMPOSE_USE_LLM=true
# RAG_TOP_K=10
# RAG_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# CHROMA_PERSIST_DIR=./chroma_data
# AGENTIC_LOOP_MAX_ITER=3
# OPENAI_LLM_MODEL=gpt-4o-mini
# MIN_SOURCES_FOR_VERDICT=1
# SERP_NUM_RESULTS=10
# SERP_REQUEST_TIMEOUT_SEC=15
